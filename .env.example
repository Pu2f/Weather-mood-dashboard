# Flask runtime settings
# Change this to a strong random string before real deployment
FLASK_SECRET_KEY=change-me-to-a-long-random-secret

# true/false (or 1/0, yes/no, on/off)
# For local development only
FLASK_DEBUG=true
